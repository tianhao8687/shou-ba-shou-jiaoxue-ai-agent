# Harbor AgentOps 3.6：从第一次打开软件到 20K 面试

**这是一份有台阶的教程，不是技术词典。**  
第一次学习时，只读当前阶梯。没有通过本阶检查，就不要向后翻。

## 怎么使用这份教程

学习分成四阶：

| 阶梯 | 你要做到什么 | 学习限制 |
|---|---|---|
| 第一阶 | 会操作、会讲事故故事 | 不讲英文术语，不读代码 |
| 第二阶 | 看懂软件最基本的组成 | 每课只学 1 个新词 |
| 第三阶 | 看懂生产安全与故障恢复 | 每课只学 1 个新词，并做一个实验 |
| 第四阶 | 回答 20K 岗位的连续追问 | 只组合前面学过的词，再选学高级内容 |

每课只做四件事：

1. 先看一个具体问题。
2. 用生活语言想明白。
3. 到软件里找到对应画面。
4. 回答一道过关题。

贯穿全文的事故只有一个：

> 商品后台已经更新了灯具价格，但部分用户仍看到旧价格。数据库里是新版本 v453，缓存里还是旧版本 v452。系统要找到原因，只刷新受影响的范围，并重新确认用户已经读到新版本。

---

# 第一阶：先会用，暂时不学技术词

这一阶共五课，每课约 10 分钟。不要打开代码。

## 第 1 课：这个软件到底在干什么

普通聊天工具可能说：“建议刷新缓存。”

Harbor 不会听到建议就直接动生产系统。它会完成五件事：

1. 查现场：旧数据比例、当前版本、受影响用户。
2. 查手册：公司规定这种问题应该怎样处理。
3. 提方案：只处理受影响的租户和商品类别。
4. 检查方案：目标、范围和风险是否合理。
5. 做完再查：版本是否真的从 v452 变成 v453。

今天只需要记住一句话：

> 这个软件不是替人聊天，而是让“发现问题—采取行动—确认恢复”有证据、有边界。

### 过关题

工具显示“操作成功”，为什么还不能马上宣布事故结束？

参考答案：工具可能只完成了命令，用户看到的数据不一定真的恢复，必须重新检查。

## 第 2 课：认识五种责任

把系统想象成一个维修班：

- 接警员：记录发生了什么。
- 调查员：读现场数据和维修手册。
- 安全员：检查方案会不会伤及其他用户。
- 施工员：执行获准的动作。
- 验收员：重新测量，判断是否真正恢复。

这些不是五个会聊天的机器人，而是程序里五种不同责任。分开责任的原因很简单：提出方案的人不能自己证明方案安全，施工的人也不能自己宣布验收合格。

### 过关题

如果调查员直接兼任施工员和验收员，最大的风险是什么？

参考答案：一个判断错误可能一路通过，没人独立阻止，也没人独立发现“假成功”。

## 第 3 课：第一次打开软件

先打开仓库文件夹，在终端中复制这一行并按回车：

```powershell
python scripts/quickstart.py up
```

看到 `status: passed` 和 `Harbor AgentOps is ready` 后再继续：

1. 打开 `http://127.0.0.1:8080`。
2. 选择“平台管理员”进入。
3. 先看“总览”，只观察任务数量和健康状态。
4. 再看“运行”，找到 `catalog-api 返回旧版本商品数据`。
5. 最后看“评测”，分清 105 项 fixture 回归与历史真实模型抽样。

现在不需要理解页面上的英文缩写，只需要认出三个区域：

- “运行”告诉你一次事故怎样处理。
- “知识库”告诉你参考了哪些公司手册。
- “评测”告诉你改完软件后有没有退步。

### 过关题

“运行”和“评测”有什么区别？

参考答案：“运行”处理一件具体事故；“评测”用一组预先准备的题检查整个系统能力。

练习结束后执行 `python scripts/quickstart.py down`，软件会停止，但练习数据会保留。

## 第 4 课：沿着缓存事故走一遍

在运行页面依次找出：

1. 现场现象：哪些用户读到旧数据。
2. 调查结果：数据库版本和缓存版本。
3. 参考手册：缓存陈旧的处理说明。
4. 建议动作：只刷新受影响范围。
5. 执行结果：工具做了什么。
6. 验收结果：版本和旧数据比例是否恢复。

把页面内容改写成自己的话：

> 系统发现缓存比数据库落后一个版本。它只刷新受影响用户的灯具缓存，没有清空全部缓存。刷新后重新读取数据，确认缓存已经是 v453，旧数据比例降到 0。

### 过关题

为什么“只刷新受影响范围”比“清空所有缓存”更成熟？

参考答案：影响范围更小、恢复更快，也减少对正常用户的打扰。

## 第 5 课：一分钟讲给外行听

关掉文档，按下面顺序讲一分钟：

1. 发生了什么。
2. 系统先查了什么。
3. 为什么没有立即执行。
4. 最终做了什么。
5. 怎样确认修好。

合格示例：

> 有些用户仍看到旧价格。系统先比较数据库和缓存版本，再读取公司手册，发现只有一个租户的一类商品受影响。它没有让模型直接控制生产，而是检查建议范围后才刷新。最后重新读取版本，确认旧数据消失。

## 第一阶通关门

- [ ] 能独立登录并找到缓存事故。
- [ ] 能指出调查、行动和验收三块内容。
- [ ] 能用一分钟讲完，不依赖英文词。
- [ ] 能解释为什么工具说成功不等于用户问题解决。

四项全部完成，才能进入第二阶。

---

# 第二阶：一次只认识一个基础词

从这里开始，每课只有一个新词。看到其他陌生词时先跳过，不要查百科。

## 第 6 课：网页怎样把事情交给后端

**本课唯一新词：API**

API 就像办事窗口。网页不能自己修改数据库，而是把事故资料交给后端窗口，后端处理后再返回结果。

回到项目：网页点击“新建事件”时，就是向后端窗口提交资料。

只看一个位置：[main.py](./backend/app/main.py)。搜索 `/api/health`，知道这里定义了一个窗口即可，不要求读懂函数。

### 说给外行听

> 网页不直接办事，它把资料交给后端的办事窗口。

### 过关题

为什么前端不应该直接连接数据库？

参考答案：这样权限、检查和审计都容易被绕过，所有关键操作应该经过后端窗口。

## 第 7 课：窗口为什么会拒收错误资料

先复习：API 是办事窗口。

**本课唯一新词：Schema**

Schema 是窗口的收件规则：哪些字段必须有、是什么类型、哪些多余内容不接受。

回到项目：创建事故必须有服务名和现象。模型提出工具参数时也要遵守规则，不能多塞一个危险字段。

只看一个位置：[schemas.py](./backend/app/schemas.py)。找到一个数据类，只观察字段名。

### 说给外行听

> 有窗口还不够，窗口必须有明确的收件格式。

### 过关题

数据格式通过检查，是否代表操作已经获准？

参考答案：不代表。这里只证明资料格式正确，还没有判断权限和风险。

## 第 8 课：页面关掉后，进度为什么还在

先复习：窗口收件后，会按规则检查资料。

**本课唯一新词：State**

State 就是“当前必须记住的进度”。对一次事故来说，它包括现场证据、走到哪一步、计划、审批和结果。

回到项目：刷新页面或重新登录，同一个事故仍保留所有步骤，因为进度保存在后端，而不是只放在网页里。

只看一个位置：[schemas.py](./backend/app/schemas.py) 中的运行记录字段。

### 说给外行听

> 这件事处理到哪里、已经找到什么，都要记在可靠的地方。

### 过关题

为什么等待审批的事故不能只保存在电脑内存里？

参考答案：审批可能等很久，进程可能重启；只放内存会丢失。

## 第 9 课：事情为什么必须按顺序推进

先复习：State 记录当前进度。

**本课唯一新词：Workflow**

Workflow 是事情推进的顺序和规则。Harbor 规定先调查、再检查方案、再执行、最后验收，不能从报警直接跳到写操作。

回到项目：运行页面那一排步骤就是工作顺序的可视化。

只看一个位置：[agent.py](./backend/app/agent.py)。搜索 `_node_`，知道每个函数对应一步即可。

### 说给外行听

> State 是现在记住什么，Workflow 是下一步允许往哪里走。

### 过关题

为什么模型不能自己跳过安全检查？

参考答案：关键顺序由程序规定，模型只在被允许的位置提出建议。

## 第 10 课：模型回答前为什么先查公司手册

先复习：Workflow 规定先调查再行动。

**本课唯一新词：RAG**

RAG 可以先理解成“先查资料，再回答”。模型不只依赖训练时的通用知识，还会拿到公司的运行手册和来源。

回到项目：缓存事故应该找到 `RB-310`，再根据该手册和现场版本提出建议。

只看一个位置：[knowledge](./data/knowledge)。打开 RB-310，读前两段。

### 说给外行听

> 聪明人也要先看公司维修手册，不能只凭记忆施工。

### 过关题

找到正确手册，是否保证最终动作正确？

参考答案：不保证。模型可能理解错，工具也可能失败，后面仍要检查和验收。

## 第 11 课：模型写的是建议，不是命令

先复习：模型先拿到手册和现场资料。

**本课唯一新词：Plan**

Plan 是准备执行的步骤说明。模型可以提出候选做法，但正式 Plan 必须由程序补齐准确目标、依据、风险和成功条件。

回到项目：模型说“刷新缓存”，程序把它变成“只刷新 xm-retail 的 lighting 分类，并在完成后检查 v453”。

只看运行页面的“证据约束执行计划”，不读代码。

### 说给外行听

> 模型写建议书，程序整理成可以检查的施工单。

### 过关题

模型输出合法的数据格式，为什么仍不能直接执行？

参考答案：格式正确不代表目标真实、证据有效、权限足够或风险合理。

## 第 12 课：长任务为什么交给后台待办箱

先复习：正式施工单可能要等模型和审批。

**本课唯一新词：Queue**

Queue 是持久的待办箱。后端先把任务放进去，不要求网页一直保持连接。

回到项目：新事故创建后成为一条待处理任务，即使关掉浏览器也不会消失。

只看运行页面的任务编号和状态。

### 说给外行听

> 事情先进入不会丢的待办箱，网页不用一直守着。

### 过关题

为什么长任务不适合一直占住网页请求？

参考答案：模型可能很慢，网络会断，任务还可能等待人工审批。

## 第 13 课：谁从待办箱拿任务

先复习：Queue 是持久待办箱。

**本课唯一新词：Worker**

Worker 是后台干活的进程。它从待办箱领取任务，推进事故步骤，再保存结果。

回到项目：Compose 中运行三个 Worker。一个临时停止，其他 Worker 仍可继续处理。

只看一个位置：[worker.py](./backend/app/worker.py)。读文件开头说明即可。

### 说给外行听

> Queue 是待办箱，Worker 是领取待办并干活的人。

### 过关题

三个 Worker 是否应该把同一任务各做一遍？

参考答案：不应该。下一阶会学习系统怎样决定当前由谁处理。

## 第二阶通关门

请按顺序、不看答案说出：

1. 网页把资料交到哪里？
2. 收件格式由什么限制？
3. 当前进度保存在哪里？
4. 谁规定步骤顺序？
5. 模型回答前为什么先查资料？
6. 模型建议怎样变成施工单？
7. 长任务先放在哪里？
8. 谁负责领取并处理？

能够用自己的话把八个答案串成一个故事，才进入第三阶。

---

# 第三阶：一次学一个生产机制

这一阶开始读少量代码和真实测试。每课仍只引入一个新词。

## 第 14 课：信息不全时必须关门

**本课唯一新词：Fail Closed**

意思是：无法确认安全时默认拒绝。模型引用不存在的证据、选择未知工具或风险无法判断时，系统不能“先试试看”。

项目位置：[policy.py](./backend/app/policy.py)。

实验：阅读策略测试，找出未知工具或未知证据被拒绝的断言。

面试一句话：

> 模型是概率系统，关键安全边界由确定性代码执行；不确定时默认拒绝并转人工。

过关题：只在模型指令里写“不要做危险操作”够吗？  
答案：不够，模型可能误解或被不可信文本诱导。

## 第 15 课：角色只能解决粗粒度权限

**本课唯一新词：RBAC**

意思是：按角色授予一类权限。例如只读人员只能查看，操作员可发起低风险任务，负责人可以审批特定动作。

实验：分别使用只读人员和平台管理员，比较按钮和后端拒绝结果。

面试一句话：

> 前端隐藏按钮只是体验，真正权限必须由后端按角色再次判断。

过关题：只读人员知道接口地址，能绕过网页直接执行吗？  
答案：不能，后端仍会检查角色。

## 第 16 课：一次动作只发一张施工证

**本课唯一新词：Capability**

它是一张短时间有效、只允许某个具体动作的签名施工证。它绑定租户、工具、步骤和当前计划。

项目位置：[security.py](./backend/app/security.py)。

实验：阅读远程工具测试，观察篡改参数或租户后请求被拒绝。

面试一句话：

> 登录身份管控制台入口，Capability 把真正执行权限缩到本次具体动作，模型从不持有长期生产凭据。

过关题：把“只刷新 lighting”改成“刷新全部”，能继续使用原施工证吗？  
答案：不能，动作内容变了。

## 第 17 课：批准以后不能偷换计划

**本课唯一新词：Plan Hash**

它是根据计划内容计算出的指纹。计划任何字段变化，指纹都会变化。审批票和施工证都绑定同一个指纹。

实验：在运行页面找到计划指纹；说明为什么它出现在审批和执行证据中。

面试一句话：

> 执行前重新计算计划指纹，确保批准的是 A，执行的仍然是 A。

过关题：只保存 `approved=true` 有什么问题？  
答案：计划后来可能被替换，却仍沿用旧批准。

## 第 18 课：高风险动作不能自己批准自己

**本课唯一新词：SoD**

它表示职责分离：申请、审批和执行不能都由同一个主体完成。

实验：创建高风险事故，依次验证自批被拒绝、第一票后不执行、第二个独立主体投票后才执行。

面试一句话：

> 我不仅检查票数，还检查主体是否独立、角色是否匹配、每票是否绑定当前计划指纹。

过关题：同一个账号连续投两票，数量为二，算双人审批吗？  
答案：不算。

## 第 19 课：后台工人的处理权会过期

**本课唯一新词：Lease**

Lease 是有时间限制的任务处理权。Worker 领取后要定期续期；长时间失联，其他 Worker 才能接管。

项目位置：[store.py](./backend/app/store.py)。

实验：在任务记录中找到 owner 和过期时间。

面试一句话：

> 长任务不能一直占数据库锁，所以用有限租约表示当前处理者，并通过续期确认它仍活着。

过关题：租约过期后，原 Worker 是否还能自行认为任务属于它？  
答案：不能。

## 第 20 课：旧工人复活后也不能写

**本课唯一新词：Fencing Token**

它是每次接管都会递增的隔离编号。数据库只接受最新编号的写入。

真实证据：项目停止已领取任务的 Worker，另一个 Worker 使用编号 2 接管；旧编号 1 的写入被拒绝。

面试一句话：

> Lease 判断当前所有权，递增隔离编号让存储层拒绝过期 Worker，二者不能互相替代。

过关题：为什么仅检查租约时间还不够？  
答案：旧 Worker 可能长时间暂停后恢复，仍拿着旧结果尝试写入。

## 第 21 课：同一动作重放只能产生一次效果

**本课唯一新词：Idempotency**

它表示同一个业务动作即使重复提交，最终也只产生一次效果。

项目位置：[tools.py](./backend/app/tools.py)。

真实证据：故障实验室先完成写操作，再故意返回 503；使用同一个动作编号重放后，副作用计数仍为 1。

面试一句话：

> 写请求重试必须复用稳定业务键，服务端持久去重记录，并让去重记录与业务修改保持一致。

过关题：每次重试生成新随机编号，算幂等吗？  
答案：不算，服务端会把它们当成不同动作。

## 第 22 课：超时不是普通失败

**本课唯一新词：Unknown Outcome**

它表示调用方不知道动作是否完成。网络超时可能发生在服务端提交之后，因此不能简单当作“没有执行”。

实验：用自己的话描述“缓存已刷新，但成功回复在路上丢失”。

面试一句话：

> 结果未知时先用同一业务键查询或重放，不能换键盲目再做一次。

过关题：收到 503 是否能证明服务端没有修改数据？  
答案：不能。

## 第 23 课：工具成功后必须重新验收

**本课唯一新词：Verification**

它表示从独立来源重新读取真实状态，确认预先定义的目标达到。

回到缓存事故：重新读取缓存版本和旧数据比例，而不是只相信刷新工具自己的返回值。

项目位置：[agent.py](./backend/app/agent.py) 中的验证步骤。

面试一句话：

> 我区分网络成功、工具成功和业务目标成功，只有独立观测满足计划中的条件才完成 Run。

过关题：为什么不能只用写工具自己的响应作为验收？  
答案：同一个错误可能同时制造“执行成功”和“验证成功”的假象。

## 第 24 课：无法撤销时怎样尽量恢复

**本课唯一新词：Compensation**

它表示无法像单数据库修改那样撤销时，用另一项受控动作恢复先前业务状态。

真实证据：扩容出现假成功时，系统把副本数从 5 恢复到原来的 2，并再次读取状态确认。

面试一句话：

> 只有旧状态已记录、逆操作明确、风险已纳入批准范围并且能再次验证时才自动补偿；不可逆动作转人工。

过关题：重启服务能不能用“取消重启”恢复？  
答案：不能，所以并非所有动作都有安全的自动恢复方案。

## 第 25 课：测试答案必须藏起来

**本课唯一新词：Oracle**

Oracle 是评分器掌握的正确答案。Agent 运行时不能看到它，否则满分没有意义。

回到项目：事故只提供现象和可查询现场；完成后评分器才知道正确根因、正确工具和应达到的真实状态。

面试一句话：

> 答案与 Agent 输入隔离，每个用例创建新状态，评分器在运行结束后才读取真值。

过关题：把 `expected_tool` 放进模型输入会怎样？  
答案：答案泄漏，评测失去可信度。

## 第 26 课：把考试过程密封起来

**本课唯一新词：Sealed Evaluation**

它表示运行时看不到答案，结束后再统一核对根因、工具、真实结果和安全行为。

项目证据：固定规则回归 15/15；真实 Qwen 抽样 3/3。二者必须分开，不能说成“真实模型 15/15”。

面试一句话：

> 小型受控测试证明当前样本和链路可用，不能外推成生产准确率。

过关题：真实模型三例全对，能说准确率 100% 吗？  
答案：只能说这三例通过，样本太小且分布受控。

## 第 27 课：一次任务到底慢在哪一步

**本课唯一新词：Trace**

Trace 是把一次任务经过的步骤、模型和工具按因果顺序串起来。

实验：在运行页面比较检索、模型和工具耗时，指出哪个阶段最慢。

面试一句话：

> 总耗时不能定位问题，我用 Run ID 串起每个阶段，并把模型等待和实际推理分开记录。

过关题：只记录“总耗时 80 秒”为什么不够？  
答案：无法判断是在排队、推理、审批还是工具调用中变慢。

## 第 28 课：一批请求的慢尾巴有多长

**本课唯一新词：P95**

把延迟从小到大排列，P95 表示 95% 的样本不超过这个值。它比平均值更容易暴露少量很慢的请求。

项目证据：真实 Qwen 三例的 P95 约 77 秒；这与事故系统自身的延迟指标不是同一个数。

面试一句话：

> 延迟数字必须带对象、阶段、样本和环境，不把业务 P95、检索 P95 和模型 P95 混在一起。

过关题：平均值 100 毫秒是否代表几乎所有请求都在 100 毫秒内？  
答案：不代表，少量极慢请求可能被平均值隐藏。

## 第 29 课：模型没坏，只是没有空位

**本课唯一新词：Admission Control**

它表示在进入稀缺资源前决定谁现在进入、谁等待、谁因为超过时间预算而拒绝。

真实证据：修复前三并发只完成 2 条，另 1 条 180 秒超时。现在数据库先协调多个 Worker 的模型名额，本地网关再限制为“1 个正在生成 + 有界等待队列”。前者避免多个进程一起冲模型，后者避免 HTTP 线程无限堆积。

面试一句话：

> 模型健康但容量已满时，应该在入口排队或明确拒绝，不能让所有请求一起冲进去超时；排队时间和推理时间还要分开记录。

过关题：三条都完成是否说明模型吞吐提高？  
答案：不说明，只是排队后完成率提高，单次推理速度没有变。

## 第 30 课：下游忙不过来要通知上游

**本课唯一新词：Backpressure**

它表示下游没有能力继续处理时，把压力传回上游，限制新任务进入，而不是无限堆积。本项目在队列已满时返回 429，在已入队但等待超时时返回 503，并提示上游稍后重试。

面试一句话：

> 准入控制决定一次谁能进入，背压让上游感知容量上限；这个项目用有界队列、等待预算和 429/503 把过载变成明确合同。

过关题：无限队列有什么风险？  
答案：请求虽然暂时没失败，但等待时间和内存会不断增长，最终形成更大故障。

## 第 31 课：办事记录和待办单为什么必须一起落库

**本课唯一新词：Transaction（事务）**

先说业务：前台把事故状态改成“已排队”，后台却没收到待办单，页面会永远卡住。反过来也不行：后台有待办单，却找不到对应事故。

事务像银行转账的“两边一起成功”：Harbor 用 `save_run_and_enqueue()` 在同一个数据库事务里同时更新 Run 和创建 Job。任何一步失败，两步都回滚；活动 Job 还有唯一约束，旧版本遗留的孤儿 Run 会由 reconciler 修复。

面试一句话：

> durable queue 不只是有一张 Job 表；业务状态与消息发布之间的原子性必须有明确方案，这里选择同库事务，未来跨系统可演进为 transactional outbox。

过关题：先保存 Run，再单独调用一次 `enqueue_job()`，为什么即使两行代码都写了异常处理仍不够？
答案：进程可能恰好在两次数据库提交之间崩溃，异常处理根本没有机会运行。

## 第 32 课：为什么 Windows 上需要 WSL2

**本课唯一新词：Container**

Container 是隔离的进程和文件环境。同一宿主上的 Linux 容器共享 Linux 内核，所以 Windows 需要 WSL2 提供相应环境。

回到项目：Compose 启动数据库、API、三个 Worker、故障实验室、网页入口和监控共八个容器。

面试一句话：

> 容器提高可重复部署和进程隔离，但不自动等于安全；仍需低权限用户、只读文件、最小系统权限和网络边界。

过关题：Linux 生产服务器也必须使用 WSL2 吗？  
答案：不需要，它本身已有 Linux 内核。

## 第 33 课：为什么实验服务要放进单独施工区

**本课唯一新词：Namespace（命名空间）**

先别想 Kubernetes。把公司大楼想成很多施工区：测试团队只能在“预发布区”工作，不能走错门进入“生产区”。Namespace 就是集群里的施工区边界。

项目把 `demo-api` 和 connector 放进 `harbor-sandbox`。事故即使写着 production，staging connector 也只接受这个命名空间；生产事件仍然只能读取证据并转人工。

动手看：运行 `kubectl get all -n harbor-sandbox`，确认演示负载都在同一个区。

过关题：有了 Namespace，是否自动不能读取别的区？

答案：还不够。Namespace 只是范围，下一课的权限规则才决定里面的人能做什么。

## 第 34 课：为什么施工员只有“调人数”的钥匙

**本课唯一新词：RBAC（按角色分配权限）**

生活里，空调维修员的门卡可以进机房，却不能开财务保险柜。RBAC 就是把“谁可以对什么东西做什么动作”写成门卡规则。

本项目的 connector 可以查看 `demo-api`，也可以修改副本数；但集群自己拒绝它读取 Secret、创建 Pod、进入容器、换镜像或修改权限。不是靠提示词说“请勿越权”，而是门卡根本没有那把钥匙。

动手看：`python scripts/kind-lab.py verify`。输出既要有允许项，也要验证拒绝项。

过关题：为什么只测试“scale 成功”不够？

答案：因为最小权限的重点还包括“不该做的事情确实做不了”。

## 第 35 课：软件升级为什么要有装修台账

**本课唯一新词：Migration（数据库迁移）**

一家营业中的酒店不能每次升级都推倒重建。要先记“第 1 次加了什么、第 2 次改了什么”，同一项不能施工两次，也不能让两个施工队同时改一面墙。

项目用 `schema_migrations` 记录版本和校验和。SQLite 先锁住施工现场；PostgreSQL 用数据库锁让多个 API 和 Worker 只由一个执行升级。已经完成的施工说明若被偷偷修改，程序会拒绝启动。

过关题：`CREATE TABLE IF NOT EXISTS` 为什么不等于 migration？

答案：它只说“没有就建”，没有升级顺序、历史校验、并发协调和版本兼容判断。

## 第 36 课：有备份文件为什么还不能放心

**本课唯一新词：Restore Drill（恢复演练）**

消防栓贴着“可用”标签不代表真的出水。数据库备份也一样：只有把文件恢复到另一间房，再检查关键数据，才知道它不是空文件或坏文件。

项目先生成 PostgreSQL 归档和 SHA-256 清单，再恢复到随机临时数据库，核对迁移版本、核心表行数和孤儿任务；通过后删除临时库。它默认拒绝覆盖正在使用的 `harbor` 数据库。

动手做：

```powershell
python scripts/database-ops.py backup --output backups/practice.dump
python scripts/database-ops.py verify backups/practice.dump
```

过关题：为什么不能直接恢复到当前业务库来测试？

答案：测试恢复不应破坏正在服务的数据；先在隔离目标验证，真正切换要有单独变更流程。

## 第 37 课：没有任务时怎样知道工人还活着

**本课唯一新词：Registry（进程登记表）**

Job heartbeat 只在工人正在办一张单时更新。如果今天没有单，不能因此判断工人失踪。Registry 像每五秒刷新的值班签到表，不管忙闲都记录“我还在”。

`/api/status` 会列出两个 Worker 的 ID、启动时间和最后签到。超过 20 秒没更新的记录不算在线。注意：Registry 证明进程活着，Job lease 才决定某张单归谁，两者职责不同。

过关题：一个 Worker 在 Registry 里在线，能否证明它拥有某个 Job？

答案：不能。在线状态与任务所有权必须分别验证。

## 第 38 课：105 道题全对为什么仍不是绝对安全

**本课唯一新词：Confidence Interval（置信区间）**

连续抛十次硬币都是正面，不代表硬币永远只会正面。测试也是抽样。Harbor 的 105 项 fixture 全过，但报告的 95% Wilson 区间仍是 96.47%–100%，下界没有写成 100%。

更重要的是，fixture 主要证明确定性流程与安全规则；真实 Qwen 还要用同一套 hidden oracle 做全量、多次运行。本轮模型服务没在线，所以报告明确分开两组结论。

过关题：105/105 可以证明什么、不能证明什么？

答案：能证明这 105 个隔离回归全部满足规则；不能证明未知事故和真实模型在生产分布上永不失败。

## 第 39 课：为什么还要找别人出的题

**本课唯一新词：Holdout（留出集）**

先不背英文。你可以把数据想成驾校题：

- 练习题可以拿来学和改方法；
- 模拟题可以用来选择参数；
- 最后一套题先锁起来，只在方法定好后考试。

最后那套锁起来的题就是“留出集”。如果做完看答案、改完方法再做同一套，它就不再是可靠的留出集。

Harbor 这次实际下载三方数据：BGL 超算日志、NAB 真实时序、AIOps Challenge 2025 故障案例。每个文件都记录固定提交、大小和 SHA-256；可以把 SHA-256 暂时理解为“文件指纹”，内容改一个字，指纹也会变。

具体业务例子：BGL 的 2,000 行日志不是全部拿来训练。388 行学习、312 行选阈值，最后 1,300 行才评分；程序看日志时不能读取 `Label`、`EventId` 和标准模板。最终 F1 是 90.22%，遇到训练阶段没见过的模板则降到 75%。这比只说“准确率很高”更接近真实工作。

再看反例：NAB 的事件召回是 83.33%，但告警精确率只有 21.85%。大白话就是“多数事故能喊出来，可喊的很多次是误报”。所以它只能生成只读线索，不能自动修复。

过关题：为什么第一版对 AIOps 400 个轻量案例全部安全转人工，仍不能说根因诊断 100%？

答案：当时输入只有时间窗，没有完整日志、指标和调用链。全部转人工只证明“不瞎猜、不乱写”，没有测根因是否猜对。3.6 又下载了完整遥测并做了一轮独立盲测，下面十课会一步一步解释。

## 第 40 课：一次事故为什么要看三台“记录仪”

**本课唯一新词：Telemetry（遥测）**

先把它理解成事故现场的记录。Harbor 同时看三类记录：日志像值班员写下的异常文字，指标像心电图，调用链像快递的中转轨迹。只看一类，很容易把“哪里出现症状”当成“哪里最先出故障”。

具体例子：支付请求变慢时，日志可能在前台服务报超时，指标显示 TiKV 延迟升高，调用链则显示等待发生在数据库边。三份记录互相印证，才形成可以复查的证据。

过关题：日志出现 `timeout`，为什么不能立刻认定写日志的服务是根因？

答案：它可能只是最先把下游故障写出来；还要用指标和调用链确认故障实体与传播方向。

## 第 41 课：同一事故为什么会差八小时

**本课唯一新词：Timezone（时区）**

可以把时区想成不同城市的钟。AIOps 归档文件名使用中国标准时间，Parquet 数据行使用 UTC；同一时刻在两个钟上会显示不同数字。程序如果不换算，就会去错误的八小时里找证据。

Harbor 先把事故窗口统一为 UTC，再查询日志、指标和调用链。这个步骤看似普通，却是生产排障最容易出现的“数据都是真的，拼起来却全错”的问题。

过关题：文件名写 16:00，数据行里为什么可能是 08:00？

答案：它们可能描述同一时刻，只是分别使用 UTC+8 和 UTC。

## 第 42 课：五千多万行为什么不用全部塞进内存

**本课唯一新词：Parquet（列式数据文件）**

把普通表格想成一本按“行”装订的登记册；Parquet 更像把时间、服务名、错误信息分别放进抽屉。只查事故时间和服务时，不必把每个抽屉都搬出来。

Harbor 用 DuckDB 直接扫描 Parquet，只读取需要的列和时间片，并先建轻量索引。本轮四天数据共有 54,426,202 行，但单个案例只处理相邻窗口，而不是一次加载全部数据。

过关题：为什么“文件很大”不等于“程序必须占用同样大的内存”？

答案：列式扫描可以只读需要的列和片段，并在磁盘上完成筛选与聚合。

## 第 43 课：正式考试前哪一天可以练习

**本课唯一新词：Calibration（校准）**

校准集就是允许看答案的练习日。Harbor 用 6 月 9 日的 16 个案例检查时间窗、字段含义和初始阈值；发现问题时可以修方法，但不能拿最后考试日来练。

业务上，校准不是追求漂亮分数，而是确认温度计刻度有没有读反。例如 Parquet 中缺失值可能以字符串 `null` 出现，程序若把它当成真实 Pod 名，实体定位就会跑偏。

过关题：为什么校准集分数高，仍不能作为最终成绩？

答案：方法已经看过它的答案，成绩可能包含针对这批数据的适配。

## 第 44 课：模拟考为什么用完就不能再叫盲测

**本课唯一新词：Validation（验证集）**

验证集像模拟考，用来发现方案是否有系统性问题。Harbor 先后对 6 月 17 日和 18 日各 24 个未见案例生成预测，再打开答案评分；第一次只有 7/10 个门槛通过，第二次为 8/10。

两次失败暴露了真实工程问题：数据里的故障名叫 `code error`，与文档写法不完全相同；`null` 字符串遮住了节点实体；TiDB、TiKV、PD 的组件归属和多模态加分方式也不合理。答案一旦打开，这两天就只能用于复盘，不能冒充最终盲测。

过关题：验证集失败后修了代码，能否再用同一批数据宣布“首次盲测通过”？

答案：不能。它已参与开发，只能保留为迭代证据，最终成绩必须换一批未见数据。

## 第 45 课：怎样证明是“先答题、后看答案”

**本课唯一新词：Prediction Freeze（预测冻结）**

它像考试交卷：先把 24 个答案原子写入文件并计算 SHA-256 指纹，之后评分器才允许打开标准答案。预测函数没有接收答案路径，运行账本还检查答案泄漏和危险写操作。

最终 6 月 19 日预测文件的指纹是 `e1dcd9bd3764105ebd666aa6670e23ea45bc638e752cac155c66825bdaa02b53`。如果事后改一个字符，指纹就不再相同。

过关题：只在口头上说“我没偷看答案”为什么不够？

答案：可靠评测需要用程序调用顺序、接口边界、不可变预测文件和哈希留下可复查证据。

## 第 46 课：三台记录仪怎样合成一个判断

**本课唯一新词：Evidence Fusion（证据融合）**

融合不是把所有分数无限相加。Harbor 分别从日志签名、指标异常和调用链边异常取每种记录里最强的一份证据，再结合时间、实体和故障类别排序，避免某服务因为指标列很多就天然胜出。

具体例子：日志提示连接失败、数据库延迟指标突增、调用链也在同一边变慢时，它们共同抬高数据库候选；如果只有一条模糊日志，系统会保留多个候选，而不是假装非常确定。

过关题：为什么不能让一个服务的 50 个普通指标压过另一个服务的 1 条强故障证据？

答案：指标数量不等于证据强度；应先在同一模态内去重或取最强证据，再跨模态组合。

## 第 47 课：一个分数为什么不能决定能否交付

**本课唯一新词：Quality Gate（质量门）**

质量门像出厂检查单，不允许只挑最好看的数字。最终 24 个全新案例要同时满足数据哈希、案例数、规则冻结、零答案泄漏、零危险写入、多模态覆盖、实体 Top-3、故障 Top-3、严格根因和证据召回十项门槛。

6 月 19 日原始盲测为 10/10：故障 Top-3 79.17%，实体 Top-3 58.33%，严格“实体+故障都第一”29.17%。后来复现检查发现同分排序会漂移；修复后的稳定运行线为 66.67%、58.33%、25.00%，仍是 10/10。质量门说明达到预先约定的下限，不等于企业生产准确率。

过关题：为什么严格根因 29.17%，项目仍能通过本轮验证？

答案：十个门分别验证来源、隔离、安全、候选召回和证据质量；严格第一名仍是明显短板，系统只提供诊断候选，不能据此开放生产自动写操作。

## 第 48 课：两个候选同分，谁应该排前面

**本课唯一新词：Tie-breaker（并列裁决）**

把它想成两名运动员同时撞线。不能让“摄像机先扫到谁”决定冠军，必须提前约定第二规则。旧代码只按分数排序；DuckDB 并行返回谁在前面，谁就赢，所以同一批数据也可能换顺序。

修复后先比分数，分数一样再按模态、故障名、实体名和来源文件排序。这个顺序不看标准答案，可能让分数变低，却能保证工程行为稳定。

过关题：为什么不能在看过留出答案后，专门挑一个得分最高的并列规则？

答案：那等于拿期末答案制定判卷细则，会污染留出集；只能选与答案无关、提前说得清的通用规则。

## 第 49 课：为什么两次文件哈希不同，结果仍能一致

**本课唯一新词：Semantic Fingerprint（语义指纹）**

完整预测文件里有“几点生成”和“这次用了多少毫秒”。时间每次都不同，所以整个文件的 SHA-256 必然不同。真正要比较的是候选、证据、置信度等业务含义；程序只去掉 `generated_at` 和 `latency_ms`，再按固定格式计算一次 SHA-256，这就是语义指纹。

两次断网重放的完整文件哈希不同，但语义指纹同为 `0618234af68c7030…`。CI 现在会真的比较它们，不再把“各自都过门”误写成“结果完全相同”。

过关题：语义比较时能不能顺手去掉一个偶尔变化的预测实体？

答案：不能。实体是业务结果，删掉它会掩盖漂移；只允许排除事先列明、确实不属于预测含义的运行字段。

## 第三阶通关门

不要背定义。任选下面三个故障，分别讲清“发生什么—程序怎样保护—测试怎样证明”：

1. Worker A 超时后，Worker B 接管，A 又恢复。
2. 工具已经写入，但成功响应丢失。
3. 工具返回成功，但业务指标没有恢复。
4. 三个任务同时调用单例本地模型。
5. 高风险计划由申请人尝试自批。
6. connector 尝试读取 Secret 或修改 Deployment 模板。
7. 备份文件生成成功，但隔离恢复后的行数不一致。
8. 105 项评测全过，却有人声称生产准确率 100%。
9. 外部数据 9/9 门槛通过，但 NAB 告警精确率只有 21.85%。
10. 两次公开遥测盲测失败后改了规则，怎样保证最终成绩没有继续偷看答案。

能连续讲三条，并能指出对应代码或测试，才进入第四阶。

---

# 第四阶：把已经理解的内容变成面试能力

这一阶允许组合前面学过的词，但不再集中背词。

## 面试回答的五句结构

1. 先给结论。
2. 说项目中的具体场景。
3. 解释程序怎样保证。
4. 给出测试或数字证据。
5. 主动说明尚未验证的边界。

示例：

> 网络超时后我不会换编号盲重试。缓存刷新实验中，工具已提交但故意返回 503，这时属于结果未知。系统复用同一个幂等键，工具端识别动作已经完成，重放后副作用仍只有一次。这个证据来自单个 PostgreSQL 故障实验室，跨地域容灾仍需预生产验证。

## 由浅到深的 30 道项目题

### 1. 这个项目做什么

小白回答：

> 它帮助值班人员调查事故、安全执行动作，并重新确认用户问题真正恢复。

项目回答：

> 模型只负责根据手册和现场证据提出建议；程序负责状态、流程、权限、审批、执行、验收和恢复。

### 2. 为什么不是普通聊天机器人

> 普通聊天通常输入一次、输出一次。Harbor 会保存长期任务进度，读取外部资料，调用工具，遇到高风险暂停，进程故障后还能继续，并验证真实结果。

### 3. 为什么不用 Elasticsearch 和 Electron

> 当前知识库小，事务数据、租户信息和向量检索放在 PostgreSQL+pgvector 能减少一套运维系统；应用是浏览器控制台，不需要桌面系统能力，Electron 会增加打包和攻击面。技术选择来自需求，不是为了堆栈。

### 4. 模型编造工具和参数怎么办

> 模型只交候选建议。正式计划由服务端从可信工具登记和现场证据生成；格式、目标、风险和权限重新检查，未知内容默认拒绝。

### 5. 高风险计划怎样防偷换

> 两名独立主体按角色投票，申请人不能自批；每票绑定计划指纹和版本，施工证也绑定同一指纹，执行前再次核对。

### 6. 三个 Worker 怎样避免重复处理

> 数据库原子领取任务，Worker 获得有限租约和递增隔离编号。租约过期后新 Worker 接管，存储层拒绝旧编号写入。真实停 Worker 测试中编号 2 接管成功。

### 7. 工具超时以后怎么办

> 先区分明确失败和结果未知。写操作复用稳定幂等键，查询工具端记录或真实状态；不能换键再做一次。

### 8. 工具返回 200 为什么还不结束

> 200 最多证明请求或操作成功。系统重新读取独立指标，只有预先定义的业务条件通过才完成；假成功则恢复或转人工。

### 9. 评测 100 分代表什么

> v4 的 105 项 fixture 全过，证明这些隔离状态、审批、工具和对抗变体没有回归；95% Wilson 区间仍只有 96.47%–100%。本轮真实模型未在线，历史 Qwen 抽样不能和 fixture 合并成生产准确率。

### 10. 本地模型为什么慢，怎样处理

> 当前 CPU 模型单次约 68–77 秒。先拆分排队与推理，再做有界准入；实测三请求从 2/3 变成 3/3，但吞吐没有提高。下一步需要容量曲线、GPU 副本或大小模型路由。

### 11. 你怎样证明项目不是玩具

> 除正常链路，还实测自批和跨租户拒绝、响应丢失、假成功、Worker 崩溃、数据库重启、Prometheus 断连、备份恢复和真实 kind RBAC。当前 Linux 测试镜像 91/91、控制面覆盖率 85.16%，遥测验证器独立 9/9、覆盖率 62.53%，密封 case 105/105；桌面与移动浏览器 6/6，kind 中审批前 1 副本、审批后 4/4 ready。覆盖率只说明哪些代码被走到，真实结果还要看故障实验和外部盲测。

### 12. 当前最大的生产差距是什么

> 已验证单节点 kind staging 的最小权限 Scale，但没有企业 production 集群、跨主机高可用、OIDC、PITR、长期 soak 和 105 项真实模型重复试验。Docker Scout 未登录，因此也不声称漏洞扫描通过。

### 13. 陌生生产事故没有实验编号时，系统会不会假装修复

> 不会。系统只允许服务端固定模板读取 Prometheus，请求方和模型都不能提交任意 PromQL。拿到带来源 URI 的指标后可以诊断，但没有生产写连接器就转人工；空数据、401 或超时则在模型调用之前停止。

### 14. Run 显示排队，但进程在创建 Job 前崩溃怎么办

> Run 状态更新和 Job 创建使用同一个数据库事务，Job 插入失败时 Run 也回滚；活动 Job 有唯一约束，reconciler 还会修复旧数据留下的孤儿 Run。这是在回答“状态与消息怎样原子发布”，不是只回答“我用了队列”。

### 15. 为什么不把 kubeconfig 直接交给 Agent

> 主控制面和模型不持有集群管理员凭据。独立 connector 用命名空间 ServiceAccount，只能读命名工作负载和写 Scale 子资源；即使模型被注入，API Server 仍拒绝 Secret、exec、模板和 RBAC 操作。

### 16. 两个服务同时启动时，谁执行数据库升级

> migration 有版本与校验和；PostgreSQL 使用事务级 advisory lock 串行升级，SQLite 使用 `BEGIN IMMEDIATE`。pgvector 冷启动另用一个锁键串行创建扩展、派生表和索引，因为 `IF NOT EXISTS` 也挡不住两个进程同时创建扩展的竞态。完成后其他副本只校验结果，版本过新或 checksum 漂移都会 fail closed。

### 17. 你怎样证明备份真的可恢复

> 不以 `pg_dump` 退出码为终点。归档先做 SHA-256 和表计数清单，再恢复到新数据库，核对 migration、核心行数和孤儿 Job，最后删除临时库；默认拒绝覆盖 live database。

### 18. Worker registry 和 Job heartbeat 有什么区别

> registry 不管忙闲都证明进程最近在线；Job heartbeat 只续某张任务的 lease。前者服务 fleet 可观测，后者服务执行所有权，不能拿一个代替另一个。

### 19. Kubernetes 扩容怎样防旧 Worker 覆盖新 Worker

> 控制面 capability 带 fencing token，connector 用 Kubernetes Lease 保存最大 token，旧 token 直接拒绝；真正更新 Scale 时还携带 resourceVersion，让 API Server 做乐观并发检查。

### 20. staging 全过为什么 production 仍然 handoff

> kind staging 只证明受控命名空间里一个 Scale 动作的权限、审批和结果验证。陌生 production 没有实验绑定或写连接器时只能读证据并转人工；把 staging 凭据扩大到 production 反而破坏最小权限。

### 21. 你怎样证明外部数据不是自己换名字造的

> 清单固定第三方仓库、40 位提交、URL、字节数、SHA-256 和许可证；CI 重新下载后逐字节核验，再断网复跑。原始文件不进 Git。日志按固定哈希分成训练、调参和留出，NAB 按整条序列隔离，预测冻结后才打开 oracle。

### 22. 外部数据 9/9 门槛通过，为什么仍不能叫生产级

> 3.5 的基础门槛只证明来源链、日志分流、时序发现和无证据安全策略；当时没有完整遥测，所以自动故障覆盖是 0%。3.6 已补四天、54,426,202 行多模态遥测，但它仍是公开数据集的离线诊断，不是企业影子流量、真实身份和生产写连接器。结论必须随证据升级，不能一步跳成“生产级”。

### 23. 五千多万行数据为什么没有把 Python 内存撑爆

> 原始数据是按日期和模态分区的 Parquet。Python 负责协议和评分，DuckDB 把时间过滤、列裁剪与聚合下推到文件扫描；每个案例只读取相邻窗口。生产上我还会补扫描字节、峰值内存和并发容量基准，不能把单机 24 案例等同于无限规模。

### 24. 你怎样防止评测程序偷看标准答案

> 预测器接口不接收 oracle，先只根据 manifest 和遥测生成预测，原子落盘并计算 SHA-256；预测冻结后，独立评分器才打开标签。报告同时记录冻结状态、oracle 泄漏次数和危险写操作数，测试还固定最终预测哈希。

### 25. 前两轮盲测为什么失败，你具体改了什么

> 第一轮 7/10、第二轮 8/10，没有删除失败记录。复盘发现四类问题：实际标签 `code error` 与文档分类不一致，字符串 `null` 掩盖真实节点，TiDB 组件映射不完整，以及多指标直接累加造成噪声偏置。修复后冻结 v3 规则，只在未见的 6 月 19 日做最终考试并取得 10/10。

### 26. 为什么这个项目仍然不用 Elasticsearch

> 这次需求是固定时间窗上的 Parquet 离线分析，DuckDB 可以直接做列裁剪和聚合；在线事务、租户和向量仍由 PostgreSQL/pgvector 承担。没有亿级在线全文检索、分片和持续摄取需求时，引入 Elasticsearch 会增加同步与运维故障域。出现那些需求后再用容量数据重新选型。

### 27. 原始故障 Top-3 79.17%，为什么严格根因只有 29.17%

> Top-3 只要求正确故障类型进入三个候选；严格根因要求实体和故障类型都排第一，条件更苛刻。这个差距说明分类已有可用召回，但实体排序仍弱，所以产品展示证据和候选，不允许模型依据第一名直接改生产。

### 28. 为什么真实遥测基线不用本地 Qwen 直接猜答案

> 首轮目标是建立可重复、可审计的下限：相同数据和规则必须得到相同语义指纹，失败能定位到字段、时间或融合规则。完整文件仍包含生成时间与实测延迟，哈希不应相同。本地 Qwen 可以作为下一层 reranker 或解释器做 A/B，但要使用新的未见集，并分别报告准确率、时延、资源和稳定性，不能覆盖确定性基线。

### 29. 十个质量门全过，能否上线自动修复

> 不能。它证明公开遥测上的离线候选、证据与评测隔离达到预设下限，而且零危险写入；没有证明目标公司的数据分布、告警延迟、身份系统、连接器最小权限和变更成功率。上线前应先只读影子运行，再经过人工审批和分阶段放量。

### 30. 这条能力下一步怎样进入真实公司

> 先接 OpenTelemetry 或企业现有日志、指标和调用链，在只读影子模式记录人工根因；再按时间切分新的盲测集，对确定性基线与本地 Qwen reranker 做 A/B；最后建立漂移、延迟、误报、人工接管和业务恢复 SLO。只有这些发布门稳定后，才为极少数可逆动作开放受审批的写权限。

### 31. 为什么报告主动把 79.17% 下调成 66.67%

> 79.17% 是先交卷后开答案的原始盲测快照，不能篡改；但交付重放发现 SQL 无序返回会改变同分候选，所以它不能继续冒充稳定运行线。修复采用与答案无关的字典序裁决，两次语义一致但 Top-3 只有 66.67%。保留两组数字比挑最好的一次更符合生产质量要求。

### 32. 冻结文件哈希和语义指纹有什么区别

> 冻结文件哈希证明某一次交卷后一个字节都没改，用于防止评分前篡改；语义指纹排除生成时间和实测耗时，用于比较两次运行的业务输出是否一致。前者应该绑定单次证据，后者才适合跨运行复现检查，两者不能互相替代。

## 选学区：一次仍只学一个高级词

只有面试官对应岗位会问时才选，不要求全部掌握。

### 选学 1：Embedding

把文本变成数字表示，让意思相近但写法不同的句子可以靠近。项目真实接入本地 Qwen3 Embedding；小型独立集与词法基线打平，因此只能说“未退化”，不能说大幅提升。

### 选学 2：Hybrid Retrieval

组合精确词搜索和语义搜索。服务名、版本号适合精确匹配，同义表达适合语义搜索，最终效果由标注集决定。

### 选学 3：Structured Output

要求模型按固定字段回答。它只保证输出形状，不保证权限和业务安全，所以后面仍需服务端检查。

### 选学 4：Tenant Isolation

每次列表、直接读取、任务、证据、审批和执行都绑定所属租户。只在前端隐藏数据不算隔离。

### 选学 5：CAS

只有数据库当前版本等于用户看到的版本才允许更新，防止旧审批页面覆盖新状态。

### 选学 6：Checkpoint

完成关键步骤后保存可恢复进度。它防进程重启后从头再来，但不能替代工具幂等。

### 选学 7：Circuit Breaker

依赖连续失败时暂时停止调用，过一段时间再试探。它处理“依赖坏了”，不能替代模型过载时的准入。

### 选学 8：HNSW

一种向量近邻索引。项目知识库小、读多写少，使用 PostgreSQL/pgvector HNSW；现有测试不代表亿级数据能力。

### 选学 9：MCP

应用与外部工具或资源服务统一交换上下文的协议。它解决接入方式，不自动完成业务权限；未来接入仍要经过 Harbor 的安全边界。

### 选学 10：Multi-Agent

多个决策角色协作。角色越多不一定越好；只有可并行、各自可验收且数据证明收益超过通信成本时才值得拆分。

## 10 分钟面试演示

1. 第 1 分钟：只讲旧价格事故。
2. 第 2 分钟：展示现场证据和公司手册。
3. 第 3 分钟：展示模型建议与正式计划的差异。
4. 第 4 分钟：展示计划指纹和最小施工证。
5. 第 5 分钟：展示双人审批。
6. 第 6 分钟：展示幂等记录。
7. 第 7 分钟：展示重新验收。
8. 第 8 分钟：讲 Worker 崩溃接管。
9. 第 9 分钟：打开“完整遥测 RCA”，讲清两次失败、预测冻结和最终 10/10 门槛。
10. 第 10 分钟：主动展示 79.17% 原始快照到 66.67% 可复现线的回落，并指出 25% 严格根因与生产影子流量仍未验证。

---

# 21 天学习安排

| 天数 | 内容 | 当天成果 |
|---|---|---|
| 1–2 | 第一阶 | 一分钟事故故事 |
| 3–6 | 第二阶，每天两课 | 画出网页、窗口、状态、流程、待办箱和后台工人 |
| 7–14 | 第三阶，每天两课左右 | 每个机制完成一道过关题 |
| 15 | 三种真实故障串讲 | Worker 崩溃、响应丢失、假成功 |
| 16 | 模型容量与评测 | 分清等待、推理和评分边界 |
| 17–18 | 前 22 道项目题 | 每题录一段 60 秒回答 |
| 19 | 按岗位选两个高级词 | 各做一张业务例子卡 |
| 20 | 完整遥测 10 课 + 第 23–32 题 | 能解释盲测隔离、失败迭代、并列裁决和两类哈希 |
| 21 | 10 分钟演示与模拟面试 | 接受连续追问并主动说边界 |

# 最终毕业检查

- [ ] 不用术语也能讲清软件价值。
- [ ] 基础词能按业务顺序串起来，而不是背定义。
- [ ] 能用三种真实失败解释系统怎样保护。
- [ ] 能指出对应页面、代码或测试。
- [ ] 能区分 fixture 105/105、历史真实模型抽样和生产准确率。
- [ ] 能解释 Namespace、RBAC、migration、恢复演练和 Worker registry 各解决什么业务问题。
- [ ] 能用“练习日—模拟考—最终考试”讲清 calibration、validation 和 holdout，且不混用成绩。
- [ ] 能解释 54,426,202 行遥测怎样扫描、预测怎样先冻结、10 个质量门分别防什么。
- [ ] 能正面回答原始盲测 79.17% / 29.17% 为什么不能替代当前可复现线 66.67% / 25.00%。
- [ ] 能主动说明尚未实跑的生产能力。

# 面试题来源与技术校验资料

公开面经只作为考察方向信号，不是企业官方题库：

- [大模型应用开发多家公司面经][S1]
- [AI 应用开发岗面经][S2]
- [淘天 AI Agent 一面][S3]
- [Agent 开发一面整理][S4]
- [美团 AI Agent 一面整理][S5]
- [AI Agent 社招二面整理][S6]

技术答案使用官方资料或论文交叉检查：

- [LangGraph 持久化说明][O1]
- [MCP 架构说明][O2]
- [AWS 安全重试与幂等][O3]
- [PostgreSQL Row Security][O4]
- [pgvector 索引说明][O5]
- [RAGAS 评测论文][O6]
- [OpenTelemetry signals][O7]
- [OWASP Excessive Agency][O8]
- [Kubernetes RBAC good practices][O9]
- [Kubernetes RBAC reference][O10]
- [kind Quick Start][O11]
- [PostgreSQL pg_dump][O12]
- [AIOps Challenge 2025 官方数据说明][O13]

[S1]: https://www.nowcoder.com/feed/main/detail/129eaa1c20444651ac3b932e200d3da4?sourceSSR=dynamic
[S2]: https://www.nowcoder.com/feed/main/detail/d792d6ebcb90491ea3e56ea950225811
[S3]: https://www.nowcoder.com/feed/main/detail/a00f89eb057d4476bd67f3b24679cdb8?urlSource=home-api
[S4]: https://www.nowcoder.com/feed/main/detail/3c5d5790b6594ef79ef271a46b7cb9af
[S5]: https://niceoffersz.com/intv/llm-agent-meituan-1st
[S6]: https://niceoffersz.com/intv/ai-agent-dev-social
[O1]: https://docs.langchain.com/oss/python/langgraph/persistence
[O2]: https://modelcontextprotocol.io/docs/learn/architecture
[O3]: https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/
[O4]: https://www.postgresql.org/docs/current/ddl-rowsecurity.html
[O5]: https://github.com/pgvector/pgvector/blob/master/README.md
[O6]: https://arxiv.org/abs/2309.15217
[O7]: https://opentelemetry.io/docs/concepts/signals/
[O8]: https://genai.owasp.org/llmrisk/llm062025-excessive-agency/
[O9]: https://kubernetes.io/docs/concepts/security/rbac-good-practices/
[O10]: https://kubernetes.io/docs/reference/access-authn-authz/rbac/
[O11]: https://kind.sigs.k8s.io/docs/user/quick-start/
[O12]: https://www.postgresql.org/docs/current/app-pgdump.html
[O13]: https://www.aiops.cn/gitlab/aiops-live-benchmark/agenticopseval/-/tree/main/AIOps2025

最后只记住：先把事故讲明白，再给做法起名字。术语是理解后的简称，不是学习的起点。
