# OPS-12 验证、重试与回滚标准

## 验证

工具返回成功不代表事故已经恢复。每个动作后必须重新读取原始症状指标，至少验证错误率、延迟或队列深度之一。

## 重试

只允许对明确的瞬时错误重试，最多两次。每次重试都保留 Trace。已经产生副作用的请求必须复用幂等键，禁止重复执行。

## 停止条件

达到最大尝试次数、证据不足、风险等级上升或验证指标恶化时，立即停止自动流程并转人工处理。

## 复盘

最终记录根因、采用的证据、工具动作、审批、验证指标和未解决风险。

