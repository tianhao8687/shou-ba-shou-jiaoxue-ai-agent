# RB-310 商品目录缓存陈旧

## 适用信号

- 商品后台价格已更新，但读取 API 仍返回旧版本。
- 数据库数据正确，缓存键版本落后。
- 只有单个租户或品类受到影响。

## 处置

优先执行只读检查：比对数据库版本和缓存版本。确认后可刷新指定租户、指定品类的缓存。精确范围的缓存刷新是低风险操作，可由 Agent 自动执行；禁止无范围地清空全部缓存。

## 验证

重新读取三个样本商品，确认版本一致；若仍不一致，停止自动动作并转人工处理。

